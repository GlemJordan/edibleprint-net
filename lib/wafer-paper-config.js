// Flat rate for the Wafer Paper — Letter Sheet format. Single source of
// truth shared by the client catalog and the server-side checkout price
// enforcement, so it's never hand-typed in more than one place.
export const WAFER_PAPER_PRICE = 12.99;
